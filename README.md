# Sample-Scrapper
This script is a job listing scraper that targets the Fuzu Kenya jobs platform. The user types a skill they want to filter out (e.g. "Java") — jobs requiring that skill will be skipped.
